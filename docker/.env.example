# Event Management System Environment Configuration
# Copy this file to .env and fill in your values

# OpenAI API Key (required for AI features)
OPENAI_APIKEY=your-openai-api-key-here

# Email Service Configuration (required for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME=Event Management System

# Optional: Database Configuration (defaults are usually fine)
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=event_management
# DB_USER=eventuser
# DB_PASSWORD=eventpass123

# Optional: Weaviate Configuration (defaults are usually fine)
# WEAVIATE_URL=http://weaviate:8080

# Optional: Kafka Configuration (defaults are usually fine)
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092
